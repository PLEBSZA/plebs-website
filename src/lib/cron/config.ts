/** Hobby plan: one scheduled run per day, up to the 300s function cap. */
export const CRON_MAX_DURATION_SECONDS = 300;
export const OUTBOX_CRON_BATCH = 50;
export const RESERVATION_CRON_BATCH = 100;
