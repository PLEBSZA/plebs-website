# Invalid as production baseline

These files (if present locally) were captured against `http://localhost:3002/`
with simulated throttling and must **not** be used to prioritise work or claim
improvements. Use `npm run perf` with a Vercel preview URL (or an explicitly
labelled local proxy) instead. See `docs/performance-measurement.md`.
